export type ThemeType = typeof light;

export const light = {
    primary: "#f45511",
    text: "#000",
    background: "red",
}
export const dark: ThemeType = {
    primary: "#f45511",
    text: "#000",
    background: "red",
}

const theme = light;
export default theme;
